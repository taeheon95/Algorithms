#include<iostream>
#include<array>
using namespace std;

void quickSort(int [], int, int);
int partition(int [], int, int);

int partition(int arr[], int low, int high)
{
    int pivot = arr[high];
    int i = (low -1);
}